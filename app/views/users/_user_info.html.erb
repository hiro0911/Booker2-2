    <h2>User info</h2>
    <!-- ・画像埋め込み。fallback→デフォルト画像のurl指定。app/assets/imagesディレクトリに置く場合、ファイル名のみ指定。 -->
    <%= attachment_image_tag user, :profile_image, :fill, 100, 100, fallback: "no_image.jpg"%>
    <table class="table">
      <tbody><tr></tr> 
      <tr>
    	  <th>name</th>
        <th><%= user.name %></th>
      </tr>
      <tr>
    	  <th>introduction</th>
    	  <th><%= user.introduction %></th>
      </tr>
</tbody></table>
<div class="row">
  <%= link_to "", edit_user_path(user.id), class: "col-xs-12 btn btn-default glyphicon glyphicon-wrench" %>
</div>